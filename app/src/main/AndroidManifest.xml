<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PainMApp"
        tools:targetApi="31">
        <activity
            android:name=".controller.activities.SummaryActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activities.SensorialSurveyActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activities.LocationActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activities.SensorialActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activities.ChooseActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PainMApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>